[package]
name = "castellan-freezer"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "cgroup v2 freezer ownership: freeze/thaw/kill session process trees."

[dependencies]
castellan-core = { path = "../castellan-core" }
nix = { workspace = true }
libc = "0.2"
