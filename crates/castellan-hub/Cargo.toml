[package]
name = "castellan-hub"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "P9.4 blast-radius-weighted trust: hub scores from the stria phrase index (grammar-free, pure Rust)."

[dependencies]
rusqlite = { version = "0.31", features = ["bundled"] }
serde = { workspace = true }
