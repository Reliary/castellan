[package]
name = "castellan-scan"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "P9.2 pluggable artifact scanner at the keep gate: findings interface, relay-vuln + semgrep adapters, baseline-delta."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
